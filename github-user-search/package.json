+ axios@<version>  
added <number> packages from <number> contributors and audited <number> packages in <time>  
"dependencies": {  
    "axios": "^<version>"  
}  
{  
  "devDependencies": {  
    "tailwindcss": "^latest_version",  
    "postcss": "^latest_version",  
    "autoprefixer": "^latest_version"  
  }  
}  
